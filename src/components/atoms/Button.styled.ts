import { styled } from "../../pages/styles/theme";

export const SubmitButton = styled("button", {
  padding: "16px 24px",
  borderRadius: "8px",
  background: "$primaryColor",
  border: "none",
  color: "#000",
  fontWeight: "600",
  cursor: "pointer",
});
