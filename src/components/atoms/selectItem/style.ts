import { styled } from '@stitches/react';

export const SelectItemContainer = styled('option', {
	all: 'unset',
	cursor: 'pointer',
	borderRadius: '8px',
	fontSize: '$normal',
	fontWeight: '400',
	color: '$dark200',
});
